<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthclub_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthClubApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ProfileActivity" android:theme="@style/ProfileTheme"></activity>
        <activity android:name=".Activities.Diet" />
        <activity android:name=".Activities.MoreVideosAndPosts" /> <!-- <activity -->
        <!-- android:name=".ui.login.MoreVideos" -->
        <!-- android:label="@string/title_activity_more_videos" /> -->
        <activity android:name=".Activities.Postdetails" />
        <activity android:name=".Activities.FullScreen" />
        <activity android:name=".Activities.LoginScreen"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>